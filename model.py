# -*- coding: utf-8 -*-
"""cropPredictionModel.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1TX_k81TCm6XBWFcl_OLhN-H0OWNav0Fw
"""

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier

data = pd.read_csv("Crop_recommendation.csv") # dataset

data.head()

data.shape

data.isnull().sum()

x = data.iloc[:,:-1]
y = data.iloc[:,-1]

x_train,x_test,y_train,y_test = train_test_split(x,y,test_size=0.2,random_state = 42)

model = RandomForestClassifier()

model.fit(x_train,y_train)

prediction = model.predict(x_test)

accuracy = model.score(x_test,y_test)

print("Accuracy is :",accuracy*100,"%")

newFeature = [[36,58,25,28.6604,59.31891,8.33,36.92]] #sample
predicted_crop = model.predict(newFeature)
print("The predicted crop is : "+ predicted_crop[0])

import pickle

with open('model.pkl','wb') as file:
  pickle.dump(model,file)

# from google.colab import files

# files.download('model.pkl')









